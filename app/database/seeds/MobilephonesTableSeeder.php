<?php

class MobilephonesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('mobilephones')->truncate();

		$mobilephones = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('mobilephones')->insert($mobilephones);
	}

}
